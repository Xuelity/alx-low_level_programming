#include <stdio.h>

/**
 * main - Entry point
 *
 * print numbers in hexadecimal
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int x;

	for (x = 0; x  <= 16; x++)
		putchar("%x");
	/*return value 0*/
	return (0);
}
