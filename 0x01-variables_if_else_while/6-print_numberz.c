#include <stdio.h>

/**
 * main - Entry Point
 *
 * print all single digits numbers using putchar
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
		putchar(x);
	putchar('\n');
	/*return 0*/
	return (0);
}
